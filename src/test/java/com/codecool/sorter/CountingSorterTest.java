package com.codecool.sorter;

import org.junit.Assert;
import org.junit.Test;

public class CountingSorterTest {

    private final Sorter sorter = new CountingSorter();

    @Test
    public void changeMyNameToMatchWhatImTesting() {
        // obviously change the assert!
        Assert.assertEquals(1, 1);
    }
}